# Open the file in vim and add the following UFW configuration:

# First, disable UFW to apply new rules
sudo ufw disable

# Add port forwarding rule: forward traffic from port 8080 to port 80
sudo ufw route allow proto tcp from any to any port 8080 comment 'Forward 8080 to 80'

# Enable UFW again
sudo ufw enable

# Check UFW status to confirm the rule is applied
sudo ufw status verbose
